tests:
  - name: unittest_fullmoon_matcher
    model: mart_fullmoon_reviews
    given:
      - input: ref('fct_reviews')
        rows:
          - {review_date: '2025-01-13'}
          - {review_date: '2025-01-14'}
          - {review_date: '2025-01-15'}
      - input: ref('seed_full_moon_dates')
        rows:
          - {full_moon_date: '2025-01-14'}
    expect:
      rows:
        - {review_date: '2025-01-13', is_full_moon: "not full moon"}
        - {review_date: '2025-01-14', is_full_moon: "not full moon"}
        - {review_date: '2025-01-15', is_full_moon: "full moon"}

  # This does not work straight forward as this .yml file is implemented in dbt v1.8+
  # and I am using older version to keep everything in dbt.
  # The work around here is to seed the output data to mysql and integrate this test as dbt_utils.equality tag in the
  # test .yml file where mart_fullmoon_reviews is defined.